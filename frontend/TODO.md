# TODO - Fleet UI implementation

## Plan confirmation
- Create Fleet layout and pages using existing theme (Tailwind warm/notion variables).

## Steps
1. Create missing fleet pages and vehicles components.
2. Create `src/services/vehicleService.js` with in-memory/mock backend.
3. Add routing in `src/App.jsx` for `/fleet/*`.
4. Ensure styling matches existing UI conventions (warm borders/cards/rounded/shadows).
5. Run `npm run dev` and verify navigation works.

